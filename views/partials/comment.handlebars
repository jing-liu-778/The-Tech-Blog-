<div class="comments">
  {{#each blog.comments as |comment|}}
    <section class="comment">
      <div class="meta">
        {{user.name}}
        on
        {{format_date date_created}}
      </div>
      <div class="text">
        {{comment_text}}
      </div>
    </section>
  {{/each}}
</div>